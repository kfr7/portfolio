Optional: replace the demo hiking images used on the homepage by adding your own files and updating `mediaMoments` in `src/pages/index.astro`.

Current site uses hosted image URLs so images always resolve.
